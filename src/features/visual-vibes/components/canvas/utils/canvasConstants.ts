/** Viewbox width used by the SVG canvas coordinate system. */
export const CANVAS_VIEWPORT_WIDTH = 1200;

/** Viewbox height used by the SVG canvas coordinate system. */
export const CANVAS_VIEWPORT_HEIGHT = 720;

/** Minimum supported canvas zoom multiplier. */
export const MIN_ZOOM = 0.25;

/** Maximum supported canvas zoom multiplier. */
export const MAX_ZOOM = 2.5;

/** Horizontal offset used when routing vertical edges around nodes. */
export const SIDE_ROUTE_OFFSET = 72;

/** Label offset applied to side-routed vertical edge labels. */
export const SIDE_ROUTE_LABEL_OFFSET = 70;

/** Pixel tolerance used when comparing edge coordinates. */
export const SIDE_ROUTE_EPSILON = 1;

/** Vertical label offset for mostly horizontal edges. */
export const HORIZONTAL_LABEL_Y_OFFSET = 18;
