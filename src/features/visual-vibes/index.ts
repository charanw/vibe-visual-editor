/**
 * Public entry point for the Visual Vibes feature.
 *
 * Keep route-level imports pointed here so feature internals can move without
 * forcing consumers to know about components, hooks, panes, or utilities.
 */
export { VisualVibesEditor } from "./components/VisualVibesEditor";
